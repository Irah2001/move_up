# Example environment variables for Move Up backend
# Copy to `.env` and fill values (do NOT commit your real .env to git)

# Server
PORT=3000
NODE_ENV=development

GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-pro-preview
